# NodeJS
node_modules/

# Project-specific
dist/
staging/

# Paper-driven backfill — regenerable intermediates.
# The aggregate `reports/extracted-paper-fields-<Letter>.json` and
# the findings markdown are the durable records.
reports/extractions/
reports/extraction-queue-*.json
reports/extraction-prompts-*.jsonl
reports/extracted-paper-fields-*-dryrun.json

# MacOS
.DS_Store